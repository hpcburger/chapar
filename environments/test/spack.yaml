# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
#
spack:
  # Include centralized configuration and specs
  include:
  - ../specs/config.yaml
  - ../specs/global.yaml

  # Test environment settings - minimal for validation
  view: false
  concretizer:
    unify: when_possible
    
  # Use system compilers for testing (detected automatically)
  # Uncomment to use centralized compilers:
  # include:
  # - ../specs/compilers.yaml